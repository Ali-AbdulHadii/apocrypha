[package]
name = "apoc-modengine"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Parses and normalizes mod archives (Fluffy AIO / FOMOD / loose) into the canonical ModBundle."

[dependencies]
apoc-domain = { path = "../apoc-domain" }
zip = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
regex = "1"
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
zip = { workspace = true }
